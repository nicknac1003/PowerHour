using UnityEngine;

public static class GlobalConstants
{
    public static Vector3 forward = new Vector3(1, 0, 1).normalized;
    public static Vector3 right   = new Vector3(1, 0, -1).normalized;
}